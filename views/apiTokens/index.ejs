<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>API Tokens - LegacyKeeper</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .page-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }
    .page-header h2 {
      margin: 0;
      color: var(--text-primary);
    }
    .info-banner {
      background: rgba(74, 144, 226, 0.1);
      border-left: 4px solid var(--accent-primary);
      padding: 1rem 1.5rem;
      border-radius: 5px;
      margin-bottom: 2rem;
    }
    .info-banner h3 {
      margin: 0 0 0.5rem 0;
      color: var(--accent-primary);
      font-size: 1rem;
    }
    .info-banner p {
      margin: 0;
      color: var(--text-secondary);
      font-size: 0.9rem;
    }
    .generate-form {
      background: var(--bg-tertiary);
      padding: 1.5rem;
      border-radius: 8px;
      margin-bottom: 2rem;
    }
    .generate-form h3 {
      margin: 0 0 1rem 0;
      color: var(--accent-primary);
    }
    .form-row {
      display: grid;
      grid-template-columns: 2fr 1fr auto;
      gap: 1rem;
      align-items: end;
    }
    .tokens-list {
      background: var(--bg-secondary);
      border-radius: 8px;
      overflow: hidden;
    }
    .tokens-header {
      background: var(--bg-tertiary);
      padding: 1rem 1.5rem;
      border-bottom: 2px solid var(--border-color);
      font-weight: 600;
      color: var(--text-primary);
    }
    .token-item {
      padding: 1.5rem;
      border-bottom: 1px solid var(--border-color);
      display: grid;
      grid-template-columns: 2fr 1fr 1fr auto;
      gap: 1rem;
      align-items: center;
    }
    .token-item:last-child {
      border-bottom: none;
    }
    .token-item:hover {
      background: var(--bg-tertiary);
    }
    .token-name {
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 0.25rem;
    }
    .token-created {
      font-size: 0.85rem;
      color: var(--text-secondary);
    }
    .token-status {
      display: inline-block;
      padding: 0.25rem 0.75rem;
      border-radius: 12px;
      font-size: 0.85rem;
      font-weight: 600;
    }
    .token-status.active {
      background: rgba(40, 167, 69, 0.1);
      color: var(--success-color);
    }
    .token-status.revoked {
      background: rgba(220, 53, 69, 0.1);
      color: var(--error-color);
    }
    .token-status.expired {
      background: rgba(255, 193, 7, 0.1);
      color: #ff9800;
    }
    .token-expiry {
      font-size: 0.85rem;
      color: var(--text-secondary);
    }
    .token-actions {
      display: flex;
      gap: 0.5rem;
    }
    .empty-state {
      text-align: center;
      padding: 4rem 2rem;
      color: var(--text-secondary);
    }
    .empty-state-icon {
      font-size: 4rem;
      margin-bottom: 1rem;
      opacity: 0.5;
    }
    .new-token-modal {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.7);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 1000;
    }
    .new-token-content {
      background: var(--bg-secondary);
      padding: 2rem;
      border-radius: 8px;
      max-width: 600px;
      width: 90%;
    }
    .new-token-content h3 {
      margin: 0 0 1rem 0;
      color: var(--accent-primary);
    }
    .token-display {
      background: var(--bg-tertiary);
      padding: 1rem;
      border-radius: 5px;
      font-family: monospace;
      word-break: break-all;
      margin: 1rem 0;
      position: relative;
    }
    .copy-btn {
      position: absolute;
      top: 0.5rem;
      right: 0.5rem;
      padding: 0.25rem 0.75rem;
      background: var(--accent-primary);
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 0.85rem;
    }
    .copy-btn:hover {
      background: var(--accent-secondary);
    }
    .warning-box {
      background: rgba(255, 193, 7, 0.1);
      border-left: 4px solid #ff9800;
      padding: 1rem;
      border-radius: 5px;
      margin: 1rem 0;
    }
    .warning-box strong {
      color: #ff9800;
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="container">
    <%- include('../partials/alerts') %>

    <div class="page-header">
      <h2>API Tokens</h2>
    </div>

    <div class="info-banner">
      <h3>What are API Tokens?</h3>
      <p>API tokens allow you to authenticate and access LegacyKeeper services programmatically. Use these tokens to integrate with third-party applications, automation scripts, or custom tools.</p>
    </div>

    <!-- Generate New Token Form -->
    <div class="generate-form">
      <h3>Generate New Token</h3>
      <form action="/api-tokens/generate" method="POST">
        <div class="form-row">
          <div class="form-group" style="margin: 0;">
            <label for="TokenName">Token Name:</label>
            <input type="text" id="TokenName" name="TokenName" class="form-control" placeholder="e.g., Mobile App, Automation Script" required>
          </div>
          <div class="form-group" style="margin: 0;">
            <label for="ExpiresIn">Expires In:</label>
            <select id="ExpiresIn" name="ExpiresIn" class="form-control">
              <option value="7d">7 days</option>
              <option value="30d">30 days</option>
              <option value="90d" selected>90 days</option>
              <option value="1y">1 year</option>
              <option value="never">Never</option>
            </select>
          </div>
          <button type="submit" class="btn btn-primary">Generate Token</button>
        </div>
      </form>
    </div>

    <!-- New Token Display Modal -->
    <% if (typeof newToken !== 'undefined' && newToken) { %>
      <div class="new-token-modal" id="newTokenModal">
        <div class="new-token-content">
          <h3>Your New API Token</h3>
          <div class="warning-box">
            <strong>Important:</strong> Copy this token now. For security reasons, you won't be able to see it again.
          </div>
          <div class="token-display">
            <code id="tokenValue"><%= newToken.token %></code>
            <button class="copy-btn" onclick="copyToken()">Copy</button>
          </div>
          <p style="color: var(--text-secondary); font-size: 0.9rem;">
            <strong>Token Name:</strong> <%= newToken.name %>
          </p>
          <div style="margin-top: 1.5rem; text-align: right;">
            <button class="btn btn-primary" onclick="closeModal()">I've Saved My Token</button>
          </div>
        </div>
      </div>
    <% } %>

    <!-- Tokens List -->
    <% if (tokens.length === 0) { %>
      <div class="tokens-list">
        <div class="empty-state">
          <div class="empty-state-icon">🔑</div>
          <p>No API tokens yet.</p>
          <p style="font-size: 0.9rem;">Generate your first token to start using the LegacyKeeper API.</p>
        </div>
      </div>
    <% } else { %>
      <div class="tokens-list">
        <div class="tokens-header">
          Your API Tokens
        </div>
        <% tokens.forEach(token => {
          const now = new Date();
          const expired = token.ExpiresAt && new Date(token.ExpiresAt) < now;
          const status = token.Status === 'revoked' ? 'revoked' : expired ? 'expired' : 'active';
        %>
          <div class="token-item">
            <div>
              <div class="token-name"><%= token.TokenName %></div>
              <div class="token-created">
                Created <%= new Date(token.CreatedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                <% if (token.LastUsedAt) { %>
                  • Last used <%= new Date(token.LastUsedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                <% } else { %>
                  • Never used
                <% } %>
              </div>
            </div>
            <div>
              <span class="token-status <%= status %>">
                <%= status === 'active' ? 'Active' : status === 'revoked' ? 'Revoked' : 'Expired' %>
              </span>
            </div>
            <div class="token-expiry">
              <% if (token.ExpiresAt) { %>
                Expires <%= new Date(token.ExpiresAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
              <% } else { %>
                Never expires
              <% } %>
            </div>
            <div class="token-actions">
              <% if (status === 'active') { %>
                <form action="/api-tokens/<%= token.UniqueId %>/revoke" method="POST" style="display: inline;">
                  <button type="submit" class="btn btn-danger btn-small" onclick="return confirm('Revoke this token? Applications using it will no longer be able to access your account.');">Revoke</button>
                </form>
              <% } %>
              <form action="/api-tokens/<%= token.UniqueId %>?_method=DELETE" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-secondary btn-small" onclick="return confirm('Delete this token permanently?');">Delete</button>
              </form>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>

    <div style="margin-top: 2rem; padding: 1.5rem; background: var(--bg-tertiary); border-radius: 8px;">
      <h3 style="margin: 0 0 1rem 0; color: var(--accent-primary);">Using Your API Token</h3>
      <p style="color: var(--text-secondary); margin-bottom: 1rem;">
        Include your API token in the Authorization header of your HTTP requests:
      </p>
      <div style="background: var(--bg-secondary); padding: 1rem; border-radius: 5px; font-family: monospace; font-size: 0.9rem;">
        <code>Authorization: Bearer YOUR_API_TOKEN</code>
      </div>
      <p style="color: var(--text-secondary); margin-top: 1rem; font-size: 0.9rem;">
        Visit the <a href="/api-docs" style="color: var(--accent-primary);">API Documentation</a> to learn more about available endpoints and usage.
      </p>
    </div>
  </div>

  <script>
    function copyToken() {
      const tokenValue = document.getElementById('tokenValue').textContent;
      navigator.clipboard.writeText(tokenValue).then(() => {
        const btn = document.querySelector('.copy-btn');
        btn.textContent = 'Copied!';
        setTimeout(() => {
          btn.textContent = 'Copy';
        }, 2000);
      });
    }

    function closeModal() {
      document.getElementById('newTokenModal').style.display = 'none';
    }
  </script>
</body>
</html>
