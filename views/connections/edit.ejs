<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Connection - LegacyKeeper</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .connection-summary {
      background: var(--bg-tertiary);
      padding: 1.5rem;
      border-radius: 8px;
      margin: 1.5rem 0;
      display: flex;
      align-items: center;
      gap: 1rem;
    }
    .connection-avatar {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      font-weight: bold;
      color: white;
      flex-shrink: 0;
    }
    .connection-avatar img {
      width: 100%;
      height: 100%;
      border-radius: 50%;
      object-fit: cover;
    }
    .connection-info h3 {
      margin: 0 0 0.5rem 0;
      color: var(--text-primary);
    }
    .connection-type-badge {
      display: inline-block;
      padding: 0.25rem 0.75rem;
      border-radius: 12px;
      font-size: 0.85rem;
      background: var(--accent-primary);
      color: white;
      margin-right: 0.5rem;
    }
    .permissions-section {
      background: var(--bg-tertiary);
      padding: 1.5rem;
      border-radius: 5px;
      margin: 1.5rem 0;
    }
    .permissions-section h3 {
      margin-top: 0;
      color: var(--accent-primary);
    }
    .checkbox-group {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    .checkbox-item {
      display: flex;
      align-items: start;
      gap: 0.75rem;
    }
    .checkbox-item input[type="checkbox"] {
      margin-top: 0.25rem;
    }
    .checkbox-item label {
      flex: 1;
      cursor: pointer;
    }
    .checkbox-item .label-title {
      font-weight: 600;
      color: var(--text-primary);
      display: block;
      margin-bottom: 0.25rem;
    }
    .checkbox-item .label-description {
      font-size: 0.85rem;
      color: var(--text-secondary);
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="container">
    <%- include('../partials/alerts') %>

    <div class="form-container" style="max-width: 700px;">
      <h2 class="card-title">Edit Connection</h2>

      <div class="connection-summary">
        <div class="connection-avatar">
          <% if (connection.ConnectionType === 'user' && connectedEntity.ProfilePicture) { %>
            <img src="<%= connectedEntity.ProfilePicture %>" alt="<%= connectedEntity.FirstName %>">
          <% } else if (connection.ConnectionType === 'user') { %>
            <%= connectedEntity.FirstName.charAt(0) %><%= connectedEntity.LastName ? connectedEntity.LastName.charAt(0) : '' %>
          <% } else if (connection.ConnectionType === 'contact') { %>
            <%= connectedEntity.FirstName.charAt(0) %><%= connectedEntity.LastName ? connectedEntity.LastName.charAt(0) : '' %>
          <% } else { %>
            <%= connectedEntity.GroupName.charAt(0) %>
          <% } %>
        </div>
        <div class="connection-info">
          <h3>
            <% if (connection.ConnectionType === 'user' || connection.ConnectionType === 'contact') { %>
              <%= connectedEntity.FirstName %> <%= connectedEntity.LastName %>
            <% } else { %>
              <%= connectedEntity.GroupName %>
            <% } %>
          </h3>
          <div>
            <span class="connection-type-badge">
              <%= connection.ConnectionType === 'user' ? 'User' : connection.ConnectionType === 'contact' ? 'Contact' : 'Contact Group' %>
            </span>
            <% if (connection.ConnectionType === 'user' && connectedEntity.Email) { %>
              <span style="color: var(--text-secondary);"><%= connectedEntity.Email %></span>
            <% } else if (connection.ConnectionType === 'contact_group' && connectedEntity.Members) { %>
              <span style="color: var(--text-secondary);"><%= connectedEntity.Members.length %> members</span>
            <% } %>
          </div>
        </div>
      </div>

      <form action="/connections/<%= connection.UniqueId %>/edit" method="POST">
        <div class="form-group">
          <label for="RelationshipType">Relationship Type:</label>
          <select id="RelationshipType" name="RelationshipType" class="form-control">
            <option value="">-- Select Relationship --</option>
            <option value="Family" <%= connection.RelationshipType === 'Family' ? 'selected' : '' %>>Family</option>
            <option value="Friend" <%= connection.RelationshipType === 'Friend' ? 'selected' : '' %>>Friend</option>
            <option value="Colleague" <%= connection.RelationshipType === 'Colleague' ? 'selected' : '' %>>Colleague</option>
            <option value="Business Partner" <%= connection.RelationshipType === 'Business Partner' ? 'selected' : '' %>>Business Partner</option>
            <option value="Beneficiary" <%= connection.RelationshipType === 'Beneficiary' ? 'selected' : '' %>>Beneficiary</option>
            <option value="Executor" <%= connection.RelationshipType === 'Executor' ? 'selected' : '' %>>Executor</option>
            <option value="Trustee" <%= connection.RelationshipType === 'Trustee' ? 'selected' : '' %>>Trustee</option>
            <option value="Other" <%= connection.RelationshipType === 'Other' ? 'selected' : '' %>>Other</option>
          </select>
        </div>

        <div class="form-group">
          <label for="Notes">Notes:</label>
          <textarea id="Notes" name="Notes" class="form-control" rows="4" placeholder="Add any notes about this connection..."><%= connection.Notes || '' %></textarea>
        </div>

        <div class="form-group">
          <label for="ConnectionStatus">Connection Status:</label>
          <select id="ConnectionStatus" name="ConnectionStatus" class="form-control">
            <option value="active" <%= connection.ConnectionStatus === 'active' ? 'selected' : '' %>>Active</option>
            <option value="inactive" <%= connection.ConnectionStatus === 'inactive' ? 'selected' : '' %>>Inactive</option>
            <option value="suspended" <%= connection.ConnectionStatus === 'suspended' ? 'selected' : '' %>>Suspended</option>
          </select>
          <small style="color: var(--text-secondary);">Inactive connections are hidden from view but not deleted</small>
        </div>

        <div class="permissions-section">
          <h3>Connection Permissions</h3>
          <div class="checkbox-group">
            <div class="checkbox-item">
              <input type="checkbox" id="CanViewDocuments" name="CanViewDocuments" <%= connection.CanViewDocuments ? 'checked' : '' %>>
              <label for="CanViewDocuments">
                <span class="label-title">Can View Documents</span>
                <span class="label-description">Allow this connection to view your documents</span>
              </label>
            </div>

            <div class="checkbox-item">
              <input type="checkbox" id="CanViewAssets" name="CanViewAssets" <%= connection.CanViewAssets ? 'checked' : '' %>>
              <label for="CanViewAssets">
                <span class="label-title">Can View Assets</span>
                <span class="label-description">Allow this connection to view your digital and financial assets</span>
              </label>
            </div>

            <div class="checkbox-item">
              <input type="checkbox" id="NotifyOnUpdates" name="NotifyOnUpdates" <%= connection.NotifyOnUpdates ? 'checked' : '' %>>
              <label for="NotifyOnUpdates">
                <span class="label-title">Notify on Updates</span>
                <span class="label-description">Send notifications when you make updates to shared information</span>
              </label>
            </div>
          </div>
        </div>

        <div style="display: flex; gap: 1rem; margin-top: 2rem;">
          <button type="submit" class="btn btn-primary">Save Changes</button>
          <a href="/connections/<%= connection.UniqueId %>" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
