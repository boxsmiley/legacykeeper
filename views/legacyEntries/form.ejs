<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= action === 'create' ? 'Add' : 'Edit' %> Legacy Entry - LegacyKeeper</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="container">
    <div class="form-container">
      <h2 class="card-title"><%= action === 'create' ? 'Add New Legacy Entry' : 'Edit Legacy Entry' %></h2>

      <form action="<%= action === 'create' ? '/legacy-entries' : '/legacy-entries/' + legacyEntry.UniqueId + '?_method=PUT' %>" method="POST">
        <div class="form-group">
          <label for="EntryTitle">Entry Title</label>
          <input type="text" id="EntryTitle" name="EntryTitle" class="form-control" value="<%= legacyEntry ? legacyEntry.EntryTitle : '' %>" required>
        </div>

        <div class="form-group">
          <label for="LegacyEntryType">Entry Type</label>
          <select id="LegacyEntryType" name="LegacyEntryType" class="form-control" required>
            <option value="">Select Type</option>
            <option value="Memory" <%= legacyEntry && legacyEntry.LegacyEntryType === 'Memory' ? 'selected' : '' %>>Memory</option>
            <option value="Message" <%= legacyEntry && legacyEntry.LegacyEntryType === 'Message' ? 'selected' : '' %>>Message</option>
            <option value="Story" <%= legacyEntry && legacyEntry.LegacyEntryType === 'Story' ? 'selected' : '' %>>Story</option>
            <option value="Advice" <%= legacyEntry && legacyEntry.LegacyEntryType === 'Advice' ? 'selected' : '' %>>Advice</option>
            <option value="Letter" <%= legacyEntry && legacyEntry.LegacyEntryType === 'Letter' ? 'selected' : '' %>>Letter</option>
            <option value="Video" <%= legacyEntry && legacyEntry.LegacyEntryType === 'Video' ? 'selected' : '' %>>Video</option>
            <option value="Audio" <%= legacyEntry && legacyEntry.LegacyEntryType === 'Audio' ? 'selected' : '' %>>Audio</option>
            <option value="Photo" <%= legacyEntry && legacyEntry.LegacyEntryType === 'Photo' ? 'selected' : '' %>>Photo</option>
          </select>
        </div>

        <div class="form-group">
          <label for="ContentText">Content</label>
          <textarea id="ContentText" name="ContentText" class="form-control" rows="10" required><%= legacyEntry ? legacyEntry.ContentText : '' %></textarea>
          <small style="color: var(--text-secondary);">Write your legacy message, story, or advice here</small>
        </div>

        <div class="form-group">
          <label for="MediaFile">Media File URL</label>
          <input type="url" id="MediaFile" name="MediaFile" class="form-control" value="<%= legacyEntry ? legacyEntry.MediaFile : '' %>" placeholder="https://example.com/media.jpg">
          <small style="color: var(--text-secondary);">Optional: Link to video, audio, or photo file</small>
        </div>

        <div style="display: flex; gap: 1rem;">
          <button type="submit" class="btn btn-primary"><%= action === 'create' ? 'Create' : 'Update' %> Entry</button>
          <a href="/legacy-entries" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
