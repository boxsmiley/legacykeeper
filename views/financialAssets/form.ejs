<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= action === 'create' ? 'Add' : 'Edit' %> Financial Asset - LegacyKeeper</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="container">
    <div class="form-container">
      <h2 class="card-title"><%= action === 'create' ? 'Add New Financial Asset' : 'Edit Financial Asset' %></h2>

      <form action="<%= action === 'create' ? '/financial-assets' : '/financial-assets/' + financialAsset.UniqueId + '?_method=PUT' %>" method="POST">
        <div class="form-group">
          <label for="AssetName">Asset Name</label>
          <input type="text" id="AssetName" name="AssetName" class="form-control" value="<%= financialAsset ? financialAsset.AssetName : '' %>" required>
        </div>

        <div class="form-group">
          <label for="AssetType">Asset Type</label>
          <select id="AssetType" name="AssetType" class="form-control" required>
            <option value="">Select Type</option>
            <option value="Bank Account" <%= financialAsset && financialAsset.AssetType === 'Bank Account' ? 'selected' : '' %>>Bank Account</option>
            <option value="Credit Card" <%= financialAsset && financialAsset.AssetType === 'Credit Card' ? 'selected' : '' %>>Credit Card</option>
            <option value="Investment" <%= financialAsset && financialAsset.AssetType === 'Investment' ? 'selected' : '' %>>Investment</option>
            <option value="Retirement" <%= financialAsset && financialAsset.AssetType === 'Retirement' ? 'selected' : '' %>>Retirement</option>
            <option value="Insurance" <%= financialAsset && financialAsset.AssetType === 'Insurance' ? 'selected' : '' %>>Insurance</option>
            <option value="Cryptocurrency" <%= financialAsset && financialAsset.AssetType === 'Cryptocurrency' ? 'selected' : '' %>>Cryptocurrency</option>
            <option value="Loan" <%= financialAsset && financialAsset.AssetType === 'Loan' ? 'selected' : '' %>>Loan</option>
            <option value="Other" <%= financialAsset && financialAsset.AssetType === 'Other' ? 'selected' : '' %>>Other</option>
          </select>
        </div>

        <div class="form-group">
          <label for="LoginURL">Login URL</label>
          <input type="url" id="LoginURL" name="LoginURL" class="form-control" value="<%= financialAsset ? financialAsset.LoginURL : '' %>" placeholder="https://bank.example.com">
        </div>

        <div class="form-group">
          <label for="UsernameIdentifier">Username/Account Number</label>
          <input type="text" id="UsernameIdentifier" name="UsernameIdentifier" class="form-control" value="<%= financialAsset ? financialAsset.UsernameIdentifier : '' %>">
        </div>

        <div class="form-group">
          <label for="PasswordKey">Password/PIN</label>
          <input type="password" id="PasswordKey" name="PasswordKey" class="form-control" value="<%= financialAsset ? financialAsset.PasswordKey : '' %>">
          <small style="color: var(--text-secondary);">Note: In production, passwords should be encrypted</small>
        </div>

        <div class="form-group">
          <label for="Notes">Notes</label>
          <textarea id="Notes" name="Notes" class="form-control"><%= financialAsset ? financialAsset.Notes : '' %></textarea>
        </div>

        <div style="display: flex; gap: 1rem;">
          <button type="submit" class="btn btn-primary"><%= action === 'create' ? 'Add' : 'Update' %> Asset</button>
          <a href="/financial-assets" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
