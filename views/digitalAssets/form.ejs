<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= action === 'create' ? 'Add' : 'Edit' %> Digital Asset - LegacyKeeper</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="container">
    <div class="form-container">
      <h2 class="card-title"><%= action === 'create' ? 'Add New Digital Asset' : 'Edit Digital Asset' %></h2>

      <form action="<%= action === 'create' ? '/digital-assets' : '/digital-assets/' + digitalAsset.UniqueId + '?_method=PUT' %>" method="POST">
        <div class="form-group">
          <label for="AssetName">Asset Name</label>
          <input type="text" id="AssetName" name="AssetName" class="form-control" value="<%= digitalAsset ? digitalAsset.AssetName : '' %>" required>
        </div>

        <div class="form-group">
          <label for="AssetType">Asset Type</label>
          <select id="AssetType" name="AssetType" class="form-control" required>
            <option value="">Select Type</option>
            <option value="Social Media" <%= digitalAsset && digitalAsset.AssetType === 'Social Media' ? 'selected' : '' %>>Social Media</option>
            <option value="Email" <%= digitalAsset && digitalAsset.AssetType === 'Email' ? 'selected' : '' %>>Email</option>
            <option value="Cloud Storage" <%= digitalAsset && digitalAsset.AssetType === 'Cloud Storage' ? 'selected' : '' %>>Cloud Storage</option>
            <option value="Website" <%= digitalAsset && digitalAsset.AssetType === 'Website' ? 'selected' : '' %>>Website</option>
            <option value="Domain" <%= digitalAsset && digitalAsset.AssetType === 'Domain' ? 'selected' : '' %>>Domain</option>
            <option value="Subscription" <%= digitalAsset && digitalAsset.AssetType === 'Subscription' ? 'selected' : '' %>>Subscription</option>
            <option value="Other" <%= digitalAsset && digitalAsset.AssetType === 'Other' ? 'selected' : '' %>>Other</option>
          </select>
        </div>

        <div class="form-group">
          <label for="LoginURL">Login URL</label>
          <input type="url" id="LoginURL" name="LoginURL" class="form-control" value="<%= digitalAsset ? digitalAsset.LoginURL : '' %>" placeholder="https://example.com/login">
        </div>

        <div class="form-group">
          <label for="UsernameIdentifier">Username/Email</label>
          <input type="text" id="UsernameIdentifier" name="UsernameIdentifier" class="form-control" value="<%= digitalAsset ? digitalAsset.UsernameIdentifier : '' %>">
        </div>

        <div class="form-group">
          <label for="PasswordKey">Password/Key</label>
          <input type="password" id="PasswordKey" name="PasswordKey" class="form-control" value="<%= digitalAsset ? digitalAsset.PasswordKey : '' %>">
          <small style="color: var(--text-secondary);">Note: In production, passwords should be encrypted</small>
        </div>

        <div class="form-group">
          <label for="Notes">Notes</label>
          <textarea id="Notes" name="Notes" class="form-control"><%= digitalAsset ? digitalAsset.Notes : '' %></textarea>
        </div>

        <div style="display: flex; gap: 1rem;">
          <button type="submit" class="btn btn-primary"><%= action === 'create' ? 'Add' : 'Update' %> Asset</button>
          <a href="/digital-assets" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
